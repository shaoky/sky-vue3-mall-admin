// @ts-ignore
const ENVIR = process.env.NODE_ENV === 'development' ? 'DEVELOPMENT' : process.env.ENVIR.toUpperCase()

const imgBaseUrl = 'http://t.wzyyyy.com/common/image/add'

export {
    imgBaseUrl,
}